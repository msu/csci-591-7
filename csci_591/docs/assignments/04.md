# Assignment 04: Data-flow Analysis

## Description

Write a data-flow analysis tool which uses the worklist algorithm and swappable flow functions to perform various data-flow analyses on <span style="font-variant-caps: small-caps;">Where3Addr</span> programs. Your analysis tool should support a command-line interface that allows a user to specify which data-flow analysis to perform and the path to a <span style="font-variant-caps: small-caps;">Where3Addr</span> source file to analyze. The analysis tool should output the results of the analysis for all variables at each program point.

### <span style="font-variant-caps: small-caps;">Where3Addr</span>

You will need to write a parser for <span style="font-variant-caps: small-caps;">Where3Addr</span> programs. The grammar for the <span style="font-variant-caps: small-caps;">Where3Addr</span> language is provided below for reference:

$$
\begin{align*}
I &::=  &x := n \\
  &|    &x := y \\
  &|    &x := y\:op\:z \\
  &|    &\text{goto}\: n \\
  &|    &\text{if}\:x \:op_r\: 0 :\text{goto}: n \\
  &|    &\text{halt}
\end{align*}
$$

$$
\begin{align*}
&op ::=  \:&+ | - | * |\; / \\
&op_r ::= \:&< | =\\
&P \in &\mathbb{N} \rightarrow I
\end{align*}
$$

The programs are provided in text source files and the line numbers of the file are used by the goto- and if-statements. Because the language syntax does not exhibit nested structure, a handwritten parser to recognize the five forms of instructions can be quickly written and will suffice for this project. You are welcome to implement the parser however you prefer.

### Analyses

You will implement two data-flow analyses: *integer sign analysis* and *reaching definitions analysis*. These analyses should be implemented as flow functions which are provided to your implementation of the worklist algorithm. It should be possible to extend your program analysis framework with new flow functions, although only the two flow functions mentioned here are required for this assignment. Your task is to define flow functions for the two analyses which perform a precise analysis on a <span style="font-variant-caps: small-caps;">Where3Addr</span> program.

#### Integer Sign Analysis

In integer sign analysis, the goal is to determine whether each variable holds a positive, negative, or zero-valued integer. The abstract domain we will use for this analysis is similar to the domain used for zero analysis but with one extra value added so that we may distinguish between positive non-zero integers and negative non-zero integers. The abstract domain is defined as $L = \{P, N, Z, \top\}$, where $P$ and $N$ respectively represent positive and negative values.

#### Reaching Definitions Analysis

A reaching definitions analysis determines which assignments of a variable may be active at each program point. For example, in the [`prog_4.w3a`](#prog_4) program the assignment to variable `y` at either instruction 1 or instruction 5 may reach the use of `y` in instruction 4: `z := z * y`. We will use subscripts to indicate the line number where a variable assignment occurs. In the earlier example, we would use $y_1$ and $y_5$ to respectively denote the assignments to `y` at instruction 1 and instruction 5.


### Tool Usage

Your data-flow analysis tool should provide a command-line interface where both the analysis to perform and the path to the program source file are provided as arguments. After running the analysis, your tool should output a table which contains columns for the instruction number/line number, worklist, and the data-flow analysis results for each instruction. See examples from the [Program Analysis](https://cmu-program-analysis.github.io/2024/resources/program-analysis.pdf) book on pages 29 and 30.

Here is example output for analyzing the following simple program:
```
1: x := 1
2: y := -1
3: halt
```

``` console
$ tool signed path/to/prog.w3a

instr | worklist | abstract val
0     | 1,2,3    | x->T y->T
1     | 2,3      | x->P y->T
2     | 3        | x->P y->N
3     | empty    | x->P y->N
```

``` console
$ tool reaching path/to/prog.w3a

instr | worklist | abstract val
0     | 1,2,3    | empty
1     | 2,3      | x_1
2     | 3        | x_1 y_2
3     | empty    | x_1 y_2
```

### Clarifying Points
- <span style="font-variant-caps: small-caps;">Where3Addr</span> source code should include explicit line numbers and those line numbers should correspond to the actual line number. This requirement is intended to preserve a reader's sanity.
- The `halt` instruction indicates the program should stop executing and otherwise has no semantic significance.

## Example Programs

### prog_1.w3a
```
1: x := 1
2: y := 1
3: x := x - y
4: halt
```

### prog_2.w3a
```
1: x := 0
2: if x < 0 goto 5
3: x := 1
4: goto 2
5: halt
```

### prog_3.w3a
```
1: t := 1
2: x := 10
3: y := 0
4: if x = 0 goto 8
5: y := 1
6: x := x - t
7: goto 4
8: x := y
9: halt
```

### <a name="prog_4"></a>prog_4.w3a
```
1: t := 1
2: u := -1
3: y := x
4: z := 1
5: if y = 0 goto 9
6: z := z * y
7: y := y - t
8: goto 5
9: x := u
10: z := y / x
11: halt
```

## Submissions

Please provide the source code for your data-flow analysis tool, a report describing your project and instructions on how to run it, and your analysis tool's output showing the results for the provided <span style="font-variant-caps: small-caps;">Where3Addr</span> example programs.

Your tool should produce correct results for all <span style="font-variant-caps: small-caps;">Where3Addr</span> programs.

## Grading

The assignment will be graded based on:

- (70%) A data-flow analysis framework implementation using the worklist algorithm and implementations for integer sign analysis and reaching definition analysis on <span style="font-variant-caps: small-caps;">Where3Addr</span> programs.
- (20%) A written guide (two or more pages) for your implementation containing an overview of its architecture, user documentation stating how to setup the tool and run analyses, and team-member attribution stating which team members worked on which components of the tool.
- (10%) Logs (separate files or included in report) which show the output from your tool when running all analyses on all provided example programs.
